name: For each commit and PR
on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: fake work
        run: |
          set -x
          command=true
          sleep 90
          echo hi
          if $command; then
            exit 0
          else
            exit 1
          fi
